import { Metadata } from "next";
import { RecurringChildrenPage } from "@/screens/RecurringChildren";

export const metadata: Metadata = {
  title: "Recurring Invoice Children",
};

const Page = () => <RecurringChildrenPage />;

export default Page;
