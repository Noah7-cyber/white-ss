"use client";

import { ChildDetail } from "@/modules/admin/page/Children/child-detail";

// When ChildDetail needs the route id or tab from the URL, restore PageProps + params and pass through.

const ChildProfilePage = () => <ChildDetail />;

export default ChildProfilePage;
